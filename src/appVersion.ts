// App version — rewritten by CI at build time to match the derived release
// version (see .github/workflows/release.yml). Used by the in-app update
// checker. The committed value is only what local dev builds report.
export const APP_VERSION = '1.31-dev';
