# Public — safe to expose to the browser (same values the mobile app uses).
NEXT_PUBLIC_SUPABASE_URL=https://gfsfjdcxxojmctnjfuvh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Canonical public origin — authoritative base for auth redirects.
# Dev: http://localhost:3000   Prod: https://app.feyton.co.rw
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only. Never prefix these with NEXT_PUBLIC_ and never commit real
# values — put them in .env.local on the server (the deploy build picks it up).
# SUPABASE_SERVICE_ROLE_KEY=
# ANTHROPIC_API_KEY=

# Mail (Mailjet REST API) — required by /api/invite
MAILJET_API_KEY=
MAILJET_SECRET_KEY=
# Must be a sender address verified in Mailjet
INVITE_FROM_EMAIL=noreply@feyton.co.rw
INVITE_FROM_NAME=FinXAI
APP_LINK=https://github.com/feyton/finxai/releases/latest
