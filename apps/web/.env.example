# Public — safe to expose to the browser (same values the mobile app uses).
NEXT_PUBLIC_SUPABASE_URL=https://gfsfjdcxxojmctnjfuvh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Canonical public origin — authoritative base for auth redirects.
# Dev: http://localhost:3000   Prod: https://app.feyton.co.rw
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only (added in later phases — AI proxy, invite emailer, admin views).
# Never prefix these with NEXT_PUBLIC_.
# SUPABASE_SERVICE_ROLE_KEY=
# ANTHROPIC_API_KEY=
# BREVO_SMTP_LOGIN=
# BREVO_SMTP_PASSWORD=
