org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m

# Build cache + parallel live HERE, not in CI env vars: the
# ORG_GRADLE_PROJECT_org.gradle.caching form sets a *project property* rather
# than the toggle and silently does nothing ("Build cache is disabled", zero
# FROM-CACHE tasks) — measured on the bibiliya pipeline. Here they are
# unambiguous and also speed up local builds.
org.gradle.caching=true
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=false

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
hermesEnabled=true

# The app already builds against New Architecture by default (RN 0.85's
# unconditional default). Made explicit because react-native-android-widget's
# build.gradle gates its own codegen on this exact property being present —
# without it, the library skips generating its codegen JNI output while the
# app's autolinking still expects it, breaking the release CMake build.
newArchEnabled=true
